/* step to write tests
1. write test functions which comes with Jest : test()
2. it takes 2 argument 
    - 1st) name_of_test (that's what we will see in console)
    - 2nd arg) function where we implement our test (when we run our tests Jest will call this function)
*/
test("Our first test", () => {});
